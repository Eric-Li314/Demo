这个demo是 用ajax轮询数据库获取信息，短连接形式，比较耗资源，且系统存在很多bug，仅作为练习
要求聊天昵称不能相同
系统每秒一次向数据库请求消息数据

系统每隔10s更新一次用户状态，每次更新延长30s为离线时间

当用户关闭网页时，offline时间停止更新，30s后系统更新用户状态时，由于offline已经小于了当前时间戳，将用户在线标识设置为0（离线）
同时，添加一条离线提示信息到数据库